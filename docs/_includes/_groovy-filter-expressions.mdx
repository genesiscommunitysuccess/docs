import { CollapsibleSlot } from '../../examples/ui/documentationBase';


:::info
<CollapsibleSlot title="Criteria Matching" description="Criteria use groovy expressions to filter responses from the back end.">

You can use a groovy or common expression to filter a request from a datasource. Only strings that match the conditions in the expression will be returned.

For example, to filter a resource with `QUANTITY`, a number value, your criteria value could be:
- `'QUANTITY > 1000'` for greater than 1000
- `'QUANTITY < 1000'` for less than 1000
- `'QUANTITY == 1000'` for equals 1000
- `'QUANTITY != 1000'` for not equals 1000

To filter a resource with `TRADE_ID`, a string value, your criteria value could be:
- `'TRADE_ID == "TR_123"'` for equals TR_123
- `'TRADE_ID != "TR_123"'` for not equals TR_123
- `'TRADE_ID == "TR_123" || TRADE_ID == "TR_124"'` for equals TR_123 or TR_123

To specify more than one condition, separate the criteria with `&&`:

`TRADE_ID == "TR_123" && QUANTITY > 1000'` for equals TR_123 and greater than 1000

Go to our page on [criteria matching](/develop/client-capabilities/server-communications/criteria-matching/) for more details.

</CollapsibleSlot>

:::
