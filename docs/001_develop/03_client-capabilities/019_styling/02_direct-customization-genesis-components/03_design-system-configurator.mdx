This section provides high-level guidance on using the design system configurator to visually customize components and manage design tokens.

design-system-configurator.md
Focus on introducing the configurator and showing basic use cases.
Scenario Inclusion:
Add cross-references to scenarios where design tokens are modified.
Mention how the configurator can export token configurations for use in the scenarios.


### to remove later
Launch the configurator using the dsconfig script from your application.
Modify design tokens such as colors, fonts, density, borders, and more.
Save your changes into a designtokens.js file, which is automatically applied to your application.
Example Use Case:
You want to adjust the primary color, font size, and border radius across all components in your application without writing custom CSS.