---
title: 'Text Area'
sidebar_label: 'Text Area'
id: client-interaction-text-area
keywords: [interaction, interactive, input, text, area, form, data]
tags:
- interaction
- interactive
- input
- text
- form
- data
sidebar_position: 8
---

import TextAreaDemo from '../../../../examples/ui/client-capabilities/interaction/text-area.js';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Text Area



Use cases:
* Comments
* Descriptions
* Multi-line text inputs
* Complex searches

## Examples

<TextAreaDemo />

<Tabs defaultValue="genesis" values={[{ label: 'Genesis', value: 'genesis', }, { label: 'React', value: 'react', }, { label: 'Angular', value: 'angular', }]}>

  <TabItem value="genesis">

    Declaration
    ```html
    <rapid-text-area></rapid-text-area>
    ```

    Usage
    ```typescript
    @customElement({
    name: 'my-element',
    template: html`
        <rapid-text-area :value=${sync((x) => x.textAreaValue)}>
          Sample Text Area
        </rapid-text-area>
      `,
  })
    export class MyElement extends GenesisElement {
      @observable textAreaValue = '';
      textAreaValueChanged() {
         console.log(this.textAreaValue);
      }
    }
    ```

  </TabItem>
  <TabItem value="react">

  Declaration
  ```html
  <rapid-text-area></rapid-text-area>
  ```
  Usage
  ```typescript
  export function MyComponent() {
    const handleTextAreaChanged = (event) => {
      console.log({ event.target.value })
    };
    return (
      <rapid-text-area onChange={handleTextAreaChanged}></rapid-text-area>
    );
  }
  ```
</TabItem>
  <TabItem value="angular">

  Declaration
  ```html
  <rapid-text-area></rapid-text-area>
  ```

  Usage
  ```typescript
  import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
  import { FormsModule } from '@angular/forms';
  @Component({
    selector: 'my-root',
    template: `
          <rapid-text-area
            (change)="onTextAreaChange($event)"
          ></rapid-text-area>
        `,
    styleUrls: ['./my.component.css'],
    standalone: true,
    schemas: [CUSTOM_ELEMENTS_SCHEMA],
    imports: [FormsModule],
  })
  export class AppComponent {
    onTextAreaChange(event: Event) {
      const target = event.target as HTMLInputElement;
      console.log(target.value);
    }
  }
  ```
</TabItem>
</Tabs>

## API

Attributes:

| Name        | Type      | Description                                                                                                 | Example |
|-------------|-----------|-------------------------------------------------------------------------------------------------------------|----|
| appearance  | `string`  | Changes the general view of the element; this can be **filled** or **outline**                              | |
| autofocus   | `boolean` | When true, the component will be focused when the page has finished loading                                 | |
| cols        | `number`  | Defines the size of the element horizontally. **Default: 20**                                               | |
| disabled    | `boolean` | Similar to `readonly`, but with a blur on the component                                                     | |
| formId      | `string`  | Associates this component to a form. Form `id` needs to be passed. If no Id informed, then it will be associated with the ancestor form | |
| maxlength   | `number`  | The maximum number of characters allowed                                                                    | |
| minlength   | `number`  | The minimum number of characters allowed                                                                    | |
| name        | `string`  | Defines the name of the element                                                                             | |
| placeholder | `string`  | Sets placeholder text for the element, which disappears when the user starts inputting                      | `<rapid-text-area placeholder="Placeholder text">` |
| readonly    | `boolean` | If true, the user cannot change the value of this field                                                     | |
| resize      | `string`  | Allows the user to resize the element; this can be {"none","both","vertical","horizontal"}. **Default: none** | |
| rows        | `number`  | Defines the size of the element vertically. **Default: 2**                                                  | |
| value       | `string`  | The text area content                                                                                       | `<rapid-text-area :value=${sync((x) => x.text_area_value)}>` |

### Slots

| Slot | Description |
|---|---|
| start | Content before the text area input |
| end | Content after the text area input |

### Parts

| Part | Description |
|---|---|
| label   | The label for the text area. |
| control | The input for the text area |
| field   | The text area element  |

### Events fired

| Part | Type | Use | Example |
|---|---|---|---|
| change | | Emits a custom change event when the text area content changes |`<rapid-text-area @change="${(x) => x.myChangeHandler(event)}">`|

### Events listened to

This component doesn't listen to any events.

### Further details
This component is an implementation of an HTML [text-area](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/textarea) element.
