---
title: 'Button'
sidebar_label: 'Button'
id: client-interaction-button
keywords: [interaction, interactive, button, form, click, submit, data]
tags:
- interaction
- interactive
- button
- form
- click
- change
- submit
- data
sidebar_position: 8
---

import ButtonDemo from '../../../../examples/ui/client-capabilities/interaction/button.js';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Button

Implementation of a [button](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/button) component.

Use cases:
* Submit forms
* Trigger actions
* Create interactive elements
* Set options

## Example

<ButtonDemo />

<Tabs defaultValue="genesis" values={[{ label: 'Genesis', value: 'genesis', }, { label: 'React', value: 'react', }, { label: 'Angular', value: 'angular', }]}>

  <TabItem value="genesis">

    Declaration
    ```html
    <rapid-button>Button</rapid-button>
    ```

    Usage
    ```typescript
    @customElement({
      name: 'my-element',
      template: html`
      <rapid-button @click=${x => x.buttonClicked()}>
        Button
      </rapid-button>
    `,
    })
    export class MyElement extends GenesisElement {
      buttonClicked() {
        alert('Button Clicked')
      }
    }
    ```

  </TabItem>
  <TabItem value="react">

    Declaration
    ```html
    <rapid-button></rapid-button>
    ```
    Usage
    ```typescript
    export function MyComponent() {
      const handleButtonClicked = (event) => {
        console.log('Button clicked')
      };

      return (
        <rapid-button onClick={handleButtonClicked}></rapid-button>
      );
    }
    ```
  </TabItem>
  <TabItem value="angular">

    Declaration
    ```html
    <rapid-button></rapid-button>
    ```

    Usage
    ```typescript
    import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
    import { FormsModule } from '@angular/forms';
    @Component({
      selector: 'my-root',
      template: `
        <rapid-button
          (click)="onButtonClicked($event)"
        ></rapid-checkbox>
      `,
      styleUrls: ['./my.component.css'],
      standalone: true,
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
      imports: [FormsModule],
    })
    export class AppComponent {
      onButtonClicked(event: Event) {
        console.log('Button clicked');
      }
    }
    ```
  </TabItem>
</Tabs>

 DOM API

 Property and attribute binding examples for Genesis Component syntax. Closing tag omitted.

 ### Attributes

| Attribute | Type | Use | Example |
|---|---|---|---|
|autofocus|`boolean`|The boolean value of whether the button is checked. | |
|appearance|`string`|Configures the component so the user cannot alter the state via the web browser.| `<rapid-button appearance="natural">Button</rapid-button>` |
|disabled|`boolean`|Makes the component visually opaque, and configures it so that the user cannot alter the state via the web browser.| `<rapid-button disabled>Button</rapid-button>` |
|form|`string`|Associates this component with a form. The Form id must be passed. If no Id is passed, then it will be associated with the ancestor form.| |
|name|`string`|Defines the name of the element.| |
|type|`string`|Defines the mode of the button.| |
|value|`string`|Defines the value associated with name.| |

The type attribute can be:
* button (default) - performs an action when clicked
* reset - can influence a group of components associated with the same form.

# Properties

No additional properties

### Slots

| Slot | Use |
|---|---|
| start | Start slot. |
| content | The default slot. |
| end | End slot. |

### Parts

| Part |
|---|
| start |
| content |
| end |

### Events fired

| Event | Use |
|---|---|
| click | Emits a change event when the button is pressed |

### Events listened to

This component doesn't listen to any events.
