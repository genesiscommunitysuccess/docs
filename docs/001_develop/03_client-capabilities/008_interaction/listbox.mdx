---
title: 'Listbox'
sidebar_label: 'Listbox'
id: client-interaction-listbox
keywords: [ interaction, interactive, accordion, accordian, disclosure, expand, hide, collapse, fold]
tags:
- interaction
- interactive
- listbox
- option
- disclosure
- expand
- hide
- collapse
- fold
sidebar_position: 8
---

import ListboxDemo from '/examples/ui/client-capabilities/interaction/listbox.js';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

Listboxes are menus that display options and allow the user to select one. Listboxes are  the menu which displays when clicking a [`<rapid-select></rapid-select>`](../../forms/form-inputs/client-interaction-select).

Use cases:
* Constructing menus as part of form items

:::tip
The primary use of this component is to be used as part of other custom form components you wish to construct. If you simply want to use a form item which allows the user to select one value we recommend you use a [`<rapid-radio-group></rapid-radio-group>`](../../forms/form-inputs/client-interaction-radio-group) or [`<rapid-segmented-control></rapid-segmented-control>`](../../forms/form-layout/client-interaction-segmented-control/) instead.
:::

## Example
<ListboxDemo />

<Tabs defaultValue="genesis" values={[{ label: 'Genesis', value: 'genesis', }, { label: 'React', value: 'react', }, { label: 'Angular', value: 'angular', }]}>

  <TabItem value="genesis">

    Declaration
    ```html
    <rapid-listbox>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
    </rapid-listbox>
    ```

    Usage
    ```typescript
    @customElement({
      name: 'my-element',
      template: html`
        <rapid-listbox @click=${(x, ctx) => x.setSelected(ctx.event)}>
          <rapid-option value="apple">Apple</rapid-option>
          <rapid-option value="banana">Banana</rapid-option>
          <rapid-option value="strawberry">Strawberry</rapid-option>
        </rapid-listbox>
        Listbox value: ${x => x.listboxValue}
      `,
    })
    export class MyElement extends GenesisElement {
      @observable listboxValue: string;
      setSelected(event: Event & { target: { value: string } }) {
        this.listboxValue = event.target.value;
      }
    }
    ```

    <!-- TODO add link to sync -->
    :::warning
    Unlike most other Genesis components you cannot use the `sync` directive here. This is because the listbox doesn't fire events, that is intended to be controlled by the parent form element.
    :::

  </TabItem>
  <TabItem value="react">

    Declaration
    ```html
    <rapid-listbox>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
    </rapid-listbox>
    ```

    Usage
    ```typescript
    export function MyComponent() {
    const [selected, setSelected] = useState('');

    return (
      <div>
        <rapid-listbox onClick={(e) => setSelected(e.target.value)}>
          <rapid-option value="apple">Apple</rapid-option>
          <rapid-option value="banana">Banana</rapid-option>
          <rapid-option value="strawberry">Strawberry</rapid-option>
        </rapid-listbox>
        <p>Selected: {selected}</p>
      </div>)
    }
    ```
  </TabItem>
  <TabItem value="angular">

    Declaration
    ```html
    <rapid-listbox>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
      <rapid-option></rapid-option>
    </rapid-listbox>
    ```

    Usage
    ```typescript
    import { Component, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
    import { FormsModule } from '@angular/forms';
    @Component({
      selector: 'my-component',
      template: `
        <rapid-listbox (click)="setSelected($event)">
          <rapid-option value="apple">Apple</rapid-option>
          <rapid-option value="banana">Banana</rapid-option>
          <rapid-option value="strawberry">Strawberry</rapid-option>
        </rapid-listbox>
        <p>Selected: {{ selected }}</p>
      `,
      standalone: true,
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
      imports: [FormsModule],
    })
    export class MyComponent {
      selected: string = '';
      setSelected(event: any) {
        this.selected = event.target.value;
      }
    }
    ```
  </TabItem>
</Tabs>

## Accordion API

### Attributes

<table>
    <thead>
        <tr>
            <th>Attribute</th>
            <th>Type</th>
            <th>Description</th>
            <th>Example</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>expand-mode</td>
            <td><code>"single" | "multi"</code></td>
            <td>When using the <em>default</em> <code>multi</code> expand mode, the user can expand multiple sections at the same time. When using the <code>single</code> expand mode, the user can only expand one section at a time.</td>
<td>

```typescript
<rapid-accordion expand-mode="single">
```

</td>
        </tr>
    </tbody>
</table>

### Properties

This component doesn't have any properties which are not also controlled via attributes.

### Slots

| Slot | Description |
| --- | --- |
| Default | The default slot which should be filled with `<rapid-accordion-item>`. |

### Parts

| Part | Description |
| --- | --- |
| item | The slot which is populated with accordion items.|

### Events fired

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Type</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>change</td>
      <td><code>string</code></td>
      <td>Fires the event when the accordion item changes its state. The event detail is the index active item.</td>
<td>

```typescript
<rapid-accordion @change="${(x,ctx) => x.myChangeHandler(ctx.event)}">
```

</td>
    </tr>
  </tbody>
</table>

### Events listened to

This component doesn't listen to any events.

## Accordion item API

### Attributes

<table>
    <thead>
        <tr>
            <th>Attribute</th>
            <th>Type</th>
            <th>Description</th>
            <th>Example</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>heading-level</td>
            <td><code>1 - 6</code></td>
            <td>Sets the header level of the heading inside of the accordion item. This is equivalent to the <code>h1</code> - <code>h6</code> tags. The default is <code>2</code>.</td>
<td>

```typescript
<rapid-accordion-item heading-level=3>
```

</td>
        </tr>
        <tr>
            <td>expanded</td>
            <td><code>boolean</code></td>
            <td>Sets the accordion item to be expanded, allowing the contents to be visible to the user.</td>
<td>

```typescript
<rapid-accordion-item expanded>
```

</td>
        </tr>
    </tbody>
</table>

### Properties

This component doesn't have any properties which are not also controlled via attributes.

### Slots

| Slot | Description |
| --- | --- |
| Default | The default slot for accordion item content |
| start | Content which can be provided between the heading and the icon |
| end | Content which can be provided between the start slot and icon |
| heading | Content which serves as the accordion item heading and text of the expand button |
| expanded-icon | The expanded icon |
| collapsed-icon | The collapsed icon |

### Parts

| Part | Description |
| --- | --- |
| heading | Wraps the button |
| button | The button which serves to invoke the item |
| heading-content | Wraps the slot for the heading content within the button |
| icon | The icon container |
| expanded-icon | The expanded icon slot |
| collapsed-icon | The collapsed icon |
| region | The wrapper for the accordion item content |

### Events fired

<table>
    <thead>
        <tr>
            <th>Event</th>
            <th>Type</th>
            <th>Description</th>
            <th>Example</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>change</td>
            <td><code>void</code></td>
            <td>Fires the accordion's button is clicked</td>
<td>

```typescript
<rapid-accordion-item @change="${(x) => x.myChangeHandler()}">
```

</td>
        </tr>
    </tbody>
</table>

### Events listened to

T
