---
title: 'Installation'
sidebar_label: 'Installation'
sidebar_position: 2
id: fix-installation
keywords: [fix, FIX, installation, genx, add]
tags:
    - fix
    - FIX
    - installation
    - genx
    - add
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Initialising and adding to a new application

The best way to add the FIX component to a new application is to use GenX - see [GenX CLI](/docs/develop/development-environment/genx/) for more details.

For example, the following command initialises a new application called `myapp` and adds the FIX component to it.

<Tabs
    defaultValue="windows"
    values={[
        { label: 'Windows', value: 'windows', },
        { label: 'Mac and Linux', value: 'mac-and-linux', }
    ]
    }>
    <TabItem value="windows">
        ```shell title="Powershell"
        npx -y @genesislcap/genx@latest init myapp -x --no-shell; npx -y @genesislcap/genx@latest add myapp -x -s genesislcap/pbc-fix-seed
        ```
    </TabItem>

    <TabItem value= 'mac-and-linux'>
        ```shell title="Bash"
        npx -y @genesislcap/genx@latest init myapp -x --no-shell && npx -y @genesislcap/genx@latest add myapp -x -s genesislcap/pbc-fix-seed
        ```
    </TabItem>
</Tabs>

## Adding to an existing application

To add the FIX component to an existing application, run this command, which uses genx to install the component (change _app-folder-name_ to the name of your app folder):
<Tabs
    defaultValue="windows"
    values={[
        { label: 'Windows', value: 'windows', },
        { label: 'Mac and Linux', value: 'mac-and-linux', }
    ]
    }>
    <TabItem value="windows">
        ```shell title="Powershell"
        npx -y @genesislcap/genx@latest add app-folder-name -x -s genesislcap/pbc-fix-seed
        ```
    </TabItem>

    <TabItem value= 'mac-and-linux'>
        ```shell title="Bash"
        npx -y @genesislcap/genx@latest add app-folder-name -x -s genesislcap/pbc-fix-seed
        ```
    </TabItem>
</Tabs>

:::important
You can check the latest available version at [genesisnpm](https://www.npmjs.com/~genesisnpm).
:::
