# Proposed docs change: remap

Requested ACTION: ADD

**Automatic UPDATE was skipped: the generated section (359 non-space chars) is much smaller than the existing one (823) and would delete content. Review and apply this to `docs/003_build-deploy-operate/03_operate/002_commands/index.mdx` manually.**

remap.mdx` has `## Syntax`, then the parameters table is likely in `03_remap.mdx`.
But wait! The prompt says:
"Target docs file: docs/003_build-deploy-operate/03_operate/002_commands/index.mdx"
Wait, if the target file is `index.mdx`, let's look at the headings of `index.mdx` again:
`[index.mdx] ## remap`
`[index.mdx] ## RenameFields`
`[index.mdx] ## SendIt`
Wait, if `index.mdx` has `## remap`, and `03_remap
