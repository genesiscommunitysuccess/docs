---
label: Page
hide_body: false
fields:
- type: text
  name: title
  label: Title
  config:
    required: true
- type: text
  name: sidebar_label
  label: Sidebar label
  config:
    required: true
- name: sidebar_position
  type: number
  label: Order within the sidebar category
  description: ''
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
    required: true
- type: text
  name: id
  label: URL Name (ID)
  description: Part of the URL after the last slash. Should be short (1-4 words),lowercase,
    no special characters (except hyphens).
  config:
    required: true
pages:
- docs/server-reference/other-modules/cache.md
